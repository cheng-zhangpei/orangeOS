# ch4-2

![img](https://rcore-os.cn/rCore-Tutorial-Book-v3/_images/address-general.png)

虽然很简单但是你不得不说，这图画的很好

下面就是对于内外碎片产生的权衡，所以为啥这个地方很好回答了为啥我们现代的存储器基本都是分页存储，我们对于一个固定大小的内存空间，我们如果要提高这个空间的使用率，我们要尽可能避免碎片，第一个是固定大小对这个空间进行切分，这就导致内碎片，也就是每一个分片充不满。第二个是我动态的给每一个程序不同大小的段，但是随着内存不断的释放和分配，段与段之间会产生大量的外部碎片。所以这就是为啥我们使用分页系统，为了减少外碎片，我们采用固定大小的分页，为了尽可能减少内碎片，我们需要分页足够的小。这个如果自适应页面大小就可能会有外碎片，所以这个权衡真的还是很有意思

> 对于一个这样的管理系统你会想着怎么优化内存空间的使用率？关键的问题到底在啥位置？

我们可以通过修改 S 特权级的一个名为 `satp` 的 CSR 来启用分页模式，在这之后 S 和 U 特权级的访存地址会被视为一个虚拟地址，它需要经过 MMU 的地址转换变为一个物理地址，再通过它来访问物理内存；而 M 特权级的访存地址，我们可设定是内存的物理地址。 

![../_images/satp.png](https://rcore-os.cn/rCore-Tutorial-Book-v3/_images/satp.png)

satp的一个结构

- `MODE` 控制 CPU 使用哪种页表实现；
- `ASID` 表示地址空间标识符，这里还没有涉及到进程的概念，我们不需要管这个地方；
- `PPN` 存的是根页表所在的物理页号。这样，给定一个虚拟页号，CPU 就可以从三级页表的根页表开始一步步的将其映射到一个物理页号。

**RISC-V 64 架构中虚拟地址为何只有 39 位？**

在 64 位架构上虚拟地址长度确实应该和位宽一致为 64 位，但是在启用 SV39 分页模式下，只有低 39 位是真正有意义的。SV39 分页模式规定 64 位虚拟地址的 这 25 位必须和第 38 位相同，否则 MMU 会直接认定它是一个不合法的虚拟地址。通过这个检查之后 MMU 再取出低 39 位尝试将其转化为一个 56 位的物理地址。

也就是说，所有 个虚拟地址中，只有最低的 （当第 38 位为 0 时）以及最高的 （当第 38 位为 1 时）是可能通过 MMU 检查的。当我们写软件代码的时候，一个地址的位宽毋庸置疑就是 64 位，我们要清楚可用的只有最高和最低这两部分，尽管它们已经巨大的超乎想象了；而本节中我们专注于介绍 MMU 的机制，强调 MMU 看到的真正用来地址转换的虚拟地址只有 39 位。

> 剩下的估计难度没有那么大，我其实只要知道怎么开启页式存储的硬件部分，如何对虚拟地址开始抽象就好了
>
> **规范地址（高 25 位等于第 38 位，也就是符号拓展）是为了把 64 位地址空间划分成两个隔离的巨大区域（用户区和内核区），并为将来扩展页表级数保留兼容性。**
> 它不是什么硬件限制，而是一种 **软件约定** 被硬件强制检查。

![../_images/pte-rwx.png](https://rcore-os.cn/rCore-Tutorial-Book-v3/_images/pte-rwx.png)

这个是多级页表中页表项的tag的作用，用来标记现在页表项有啥意义。emmm原文中，将我们多级页表表示为一棵字典树，emmm我个人认为似乎是没必要去搞一个类似的东西。

> 大页：这个东西我记得在内存虚拟化的部分有遇到过这个东西，目的其实是为了节省地址翻译的链路。所以页面大小越大，查找链路越小，这里rCore中似乎有对大页的详细描述
>
> 在 SV39 中，如果使用了一级页索引就停下来，则它可以涵盖虚拟页号的高 位为某一固定值的所有虚拟地址，对应于一个 的大页；如果使用了二级页索引就停下来，则它可以涵盖虚拟页号的高 位为某一固定值的所有虚拟地址，对应于一个 的大页。以同样的视角，如果使用了所有三级页索引才停下来，它可以涵盖虚拟页号的高 位为某一个固定值的所有虚拟地址，自然也就对应于一个大小为 的虚拟页面。
>
> 从上面这段可以看出，如果递归不走到最深处的话，是不是后面二级页号和三级页号都可以作为我们的页内偏移？这样我们的页面大小明显更大了是吧？这样看还是很容易理解的。